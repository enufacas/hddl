#!/usr/bin/env node

/**
 * Google Cloud Logging MCP Server (Simplified)
 *
 * Provides access to Cloud Run API logs via gcloud CLI.
 * Requires: gcloud auth login
 *
 * NOTE: This is a placeholder. For production, install @modelcontextprotocol/sdk:
 * npm install @modelcontextprotocol/sdk
 *
 * For now, use the /api-monitor skill which provides similar functionality.
 */

console.error('GCP Logging MCP server placeholder - use /api-monitor skill instead');
process.exit(1);
